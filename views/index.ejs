<%- include('partials/header', { title: 'Sangisang Ordering App' }) %>
<%- include('partials/nav') %>

<section class="hero">
  <div class="hero-content">
    <h1>Sangisang Services Ordering App</h1>
    <h2>Order Gas with Ease</h2>
    <p>Check the best prices and get fast delivery from trusted sellers.</p>
    <a href="/sellers" class="cta-button">Find Sellers</a>
  </div>
</section>

<% if (user) { %>
  <p class="fix_user">Hello, <%= user.name %>! <a href="/auth/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></p>
<% } else { %>
  <p class="centreLoginRegister"><a href="/auth/login">Login</a> or <a href="/auth/register">Register</a></p>
<% } %>

<%- include('partials/footer') %>
